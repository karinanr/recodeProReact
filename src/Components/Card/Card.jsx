export default (props) => {

    return(

        <section className="divCards container-fluid">
            <div className="card">
                <img src={props.foto}alt=""/>
            </div>

        </section>
   
  
  )
  }

 